[pytest]
# Run from agent-runtime/. pythonpath=. makes the runtime's top-level
# modules (capabilities, destructive_guard, invocation_context, tools.*)
# importable without an installed package.
pythonpath = .
testpaths = tests
addopts = -q
